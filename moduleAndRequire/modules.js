// importing several different things or maybe one thing from a different file

const newFile = require('./people');

console.log(newFile);
//console.log(nw.people, nw.age);
// const { people, age } = require('./people');
// console.log(nw);

// or we can distructure

// const { people } = require('./people');

// console.log(people);

// const os = require('os'); // core modules in node modules are operating system (os) and file system (fs)
// console.log(os.platform(), os.homedir());
